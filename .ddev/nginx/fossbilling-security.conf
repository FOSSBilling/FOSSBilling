# Deny direct access to sensitive project files and directories.
location = /config.php {
    return 403;
}

location ^~ /vendor/ {
    return 403;
}

location ^~ /data/assets/gateways/ {
    try_files $uri =404;
}

location ^~ /data/ {
    return 403;
}
