{% set pending_messages = guest.system_get_pending_messages %}
{% if pending_messages %}
<script>
    const pendingMessages = {{ pending_messages|json_encode(constant('JSON_HEX_TAG'))|raw }};
    pendingMessages.forEach((message) => FOSSBilling.message(message));
</script>
{% endif %}
