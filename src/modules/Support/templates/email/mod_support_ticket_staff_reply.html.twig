{% block subject %}[{{ guest.system_company.name }}] {{ticket.subject|default('')}}{% endblock %}
{% block content %}
<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<style type="text/css">
		body {
			font-family: Arial, sans-serif;
			font-size: 14px;
			color: #333333;
		}

		h1 {
			font-size: 24px;
			font-weight: bold;
			margin: 0 0 20px;
		}

		p {
			margin: 0 0 10px;
		}

		strong {
			font-weight: bold;
		}

		.signature {
			font-style: italic;
			color: #999999;
			margin-top: 20px;
			border-top: 1px solid #cccccc;
			padding-top: 10px;
		}
	</style>
</head>
<body>
	{% set is_guest_ticket = ticket.hash|default('') %}
	{% set requester_name = ticket.author.name|default(ticket.author_name|default(c.first_name|default('') ~ ' ' ~ c.last_name|default(''))) %}
	<h1>Ticket Replied</h1>

    <p>Hi {{ requester_name }},</p>

    <p>A new reply was posted to ticket <strong>{{ticket.subject|default('')}}</strong></p>

    <p>Ticket info</p>
    <ul>
        <li><strong>Ticket ID:</strong> #{{ticket.id|default('')}}</li>
        <li><strong>Department:</strong> {{ticket.helpdesk|default({}).name|default('')}}</li>
        <li><strong>Status:</strong> {{ticket.status|default('')|title}}</li>
    </ul>

    {% if is_guest_ticket %}
        <a href='{{ "support/contact-us/conversation/#{ticket.hash|default('')}"|url(area: 'client') }}' target="_blank">Reply</a>
    {% else %}
        <a href='{{ "support/ticket/#{ticket.id|default('')}"|url(area: 'client') }}' target="_blank">Reply</a>
    {% endif %}

    <p>Please do not reply to this email directly. Use the link provided above.</p>

    <p>You may <a href="{{ 'login'|url(query: { 'email': c.email|default('') }, area: 'client') }}" target="_blank">login</a> or <a href="{{ 'client/profile'|url(area: 'client') }}" target="_blank">edit your profile.</a>

	<div class="signature">{{ guest.system_email.signature }}</div>
</body>
</html>
{% endblock %}
